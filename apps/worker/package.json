{
  "name": "@phone-automation/worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "uv build",
    "dev": "uv run python -m phone_automation_worker",
    "format": "uv run python -m compileall src tests",
    "lint": "uv run python -m compileall src tests",
    "test": "uv run pytest -q",
    "typecheck": "uv run python -m compileall src tests"
  }
}
